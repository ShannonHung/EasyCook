# Server port is arbitrary
server.port=8082

# Data Source Setup
# Note: Users must create a mysql db that they will reference here, create a user that has access to it on localhost
#       and provide the credentials below to access it
#spring.datasource.url=jdbc:mysql://localhost:3306/easyCook?serverTimezone=UTC
#spring.datasource.username=shannon
#spring.datasource.password=hung0602
#spring.datasource.initialization-mode=always
#spring.jpa.hibernate.ddl-auto=create-drop

## h2 if you want to use in-memories
spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.url=jdbc:h2:mem:easyCook

# uncomment the below lines to view sql output in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
