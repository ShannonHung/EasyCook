
# h2 if you want to use in-memories
spring:
  h2:
  console:
    enabled: true
    path: /h2
  datasource:
    url: jdbc:h2:mem:easyCook

# Data Source Setup
# Note: Users must create a mysql db that they will reference here, create a user that has access to it on localhost
#       and provide the credentials below to access it //http://140.118.110.164/
#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/easyCook?serverTimezone=UTC
#    #    url: jdbc:mysql://140.118.110.164:3366/kioxia
#    username: shannon
#    password: hung0602
#    initialization-mode: always
#  jpa:
#    # uncomment the below lines to view sql output in console
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#    hibernate.ddl-auto: create-drop

# Server port is arbitrary
server:
  port: 8082
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

# JWT
security:
  jwt:
    secret: ThisIsEasyCookSecretKeyCreatedByShannon
